This module makes it easier to test a test module.

I'm unsure if the correct approach for capturing the results and output of
Test::Builder tests, so how you initialise the test capture stuff may change
change again.

The actual comparions functions shouldn't change much at all.
